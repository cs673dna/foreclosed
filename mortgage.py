class Mortgage():
	def __init__(self, monthlyPayment, amountOwed, first, ownerOccupied, units, current):
		self.monthlyPayment = monthlyPayment
		self.amountOwed = amountOwed
		self.first = first
		self.ownerOccupied = ownerOccupied
		self.units = units
		self.current = current
